export enum ERoutes {
  SIGNIN = "/signin",
  SIGNUP = "/signup",
  PROFILE = "/profile",

  ALBUM = "/album",
  ALBUM_ITEM = "/album/:id",

  PLAYLIST = "/playlist",
  PLAYLIST_ITEM = "/playlist/:id",

  UPLOAD = "/upload",
  HOME = "/"
}
